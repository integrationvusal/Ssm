var allowSlideNext = true;function MSliderController(identifier) {		var self, slider = {}, index = 0;		init();		function init() {		self = $(identifier);		slider.items = self.find('.mslider-item');		slider.slidesCount = slider.items.length;				var segments = tmpl($('#mslider-segment-template').html(), {slidesCount : slider.slidesCount});		self.find('.mslider-top').prepend(segments);				DragMaster.onDrag(function(){}).makeDraggable(self.find('.mslider-pointer'));				setInterval(function(){			if (allowSlideNext) slideNext(index);		}, 7000);			}		function slideTo() {		var itemWidth = parseInt(slider.items.width());		self.find('.mslider-ribbon').animate({			'left': -(index * itemWidth) + 'px',		});		$('.mslider-pointer').animate({			'left': parseInt(index * parseInt($('.mslider-segment').width())) + 'px',		});	}		function slideNext() {		if (index < slider.slidesCount - 1) {			index++;		}		else {			index = 0;		}		slideTo();	}				function onResize() {			}			}