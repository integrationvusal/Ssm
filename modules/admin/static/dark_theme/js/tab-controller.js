function TabController(identifier) {	var activeButton = 0, self;		init();		function init() {		self = $(identifier);		self.find('.tab-button').click(tabButtonClicked);	}		function tabButtonClicked() {		var index = $(this).index();		$(self.find('.tab-button').get(activeButton)).removeClass('tab-button-active');		$(self.find('.tab-content').get(activeButton)).addClass('hide');				activeButton = index;				$(self.find('.tab-button').get(activeButton)).addClass('tab-button-active');		$(self.find('.tab-content').get(activeButton)).removeClass('hide');	}}